# TODO here

* Build model objects around DAO! After decided which way to follow:
   * Schema first? - so we write .sqls to create schema and generate out of DB schema, or
   * Model first? - we create the Model objects, annotate and then generate DB schema out of them, or
   * mix (yepp, also possible right?)
